# CiliAI RuoYi 迁移 - 环境准备报告

**生成时间**：2026-04-16  
**阶段**：第一阶段 - 环境准备  
**状态**：✅ 完成

---

## 1. 环境检查结果

### 1.1 JDK 环境
```
✅ JDK 24.0.1 已安装
   - 供应商：Eclipse Adoptium
   - 运行时：Temurin-24.0.1+9
   - 满足 RuoYi 要求（JDK 17+）
```

### 1.2 Maven 环境
```
✅ Maven 3.8.8 已安装
   - Maven 主目录：D:\apache-maven-3.8.8
   - Java 版本：24.0.1
   - 满足 RuoYi 要求
```

### 1.3 MySQL 环境
```
⚠️ MySQL 服务已安装但未启动
   - MySQL 服务：已停止
   - MySQL84 服务：已停止
   - 项目配置使用：localhost:3306/ciliai
   - 建议：启动 MySQL 服务或使用 Docker MySQL
```

### 1.4 Redis 环境
```
⚠️ Redis 未安装或未配置
   - RuoYi 可选功能（用于缓存）
   - 当前项目未使用 Redis
   - 如需使用可后续安装
```

---

## 2. RuoYi 项目结构

### 2.1 目录结构
```
d:\fangtang\RuoYi-Vue-master\
│
├── 📦 ruoyi-admin/                 # 后台管理后端模块（主入口）
│   ├── src/main/java/             # Java 源代码
│   │   └── com/ruoyi/
│   │       ├── web/              # Web 层（Controller）
│   │       │   ├── common/       # 通用控制器
│   │       │   ├── monitor/      # 监控模块
│   │       │   ├── system/       # 系统管理模块
│   │       │   └── tool/         # 工具模块
│   │       ├── core/             # 核心配置
│   │       └── RuoYiApplication.java  # 启动类
│   └── src/main/resources/        # 配置文件
│       ├── application.yml       # 主配置文件
│       ├── application-druid.yml # 数据库配置
│       └── mybatis/             # MyBatis 配置
│
├── 📦 ruoyi-system/               # 系统管理模块
│   ├── domain/                    # 实体类
│   ├── mapper/                   # MyBatis Mapper
│   └── service/                  # 业务服务层
│
├── 📦 ruoyi-framework/            # 框架核心
│   ├── aspectj/                  # AOP 切面
│   ├── config/                   # 配置类
│   ├── security/                # 安全认证
│   └── web/                     # Web 异常处理
│
├── 📦 ruoyi-common/              # 公共组件
│   ├── annotation/               # 注解
│   ├── constant/                 # 常量
│   ├── core/                    # 核心类
│   ├── exception/               # 异常处理
│   ├── filter/                  # 过滤器
│   ├── utils/                   # 工具类
│   └── xss/                     # XSS 防护
│
├── 📦 ruoyi-generator/           # 代码生成器
│   ├── controller/              # 生成器控制器
│   ├── domain/                  # 生成器实体
│   ├── mapper/                 # 生成器 Mapper
│   ├── service/                # 生成器服务
│   └── resources/
│       ├── vm/                 # Velocity 模板
│       └── generator.yml       # 生成器配置
│
├── 📦 ruoyi-quartz/             # 定时任务模块
│   ├── controller/              # 任务控制器
│   ├── domain/                 # 任务实体
│   ├── mapper/                 # 任务 Mapper
│   ├── service/               # 任务服务
│   └── task/                   # 任务示例
│
├── 📦 ruoyi-ui/                 # 管理后台前端（Vue 2）
│   ├── src/
│   │   ├── api/               # API 接口
│   │   ├── assets/            # 静态资源
│   │   ├── components/        # 公共组件
│   │   ├── router/            # 路由配置
│   │   └── views/             # 页面视图
│   ├── package.json           # 依赖配置
│   └── vite.config.js        # Vite 配置
│
├── 📁 sql/                     # 数据库脚本
│   ├── ry_20260321.sql        # RuoYi 基础表
│   └── quartz.sql              # Quartz 定时任务表
│
└── 📄 pom.xml                  # Maven 配置
```

### 2.2 技术栈

| 层级 | 技术 | 版本 |
|-----|------|------|
| **后端框架** | Spring Boot | 4.0.3 |
| **安全框架** | Spring Security | 6.x |
| **认证** | JWT | 0.9.1 |
| **ORM** | MyBatis | 4.0.1 |
| **数据库连接池** | Druid | 1.2.28 |
| **缓存** | Redis + Spring Data Redis | - |
| **前端框架** | Vue 2 | 2.6.12 |
| **UI 组件** | Element UI | 2.15.14 |
| **构建工具** | Vue CLI | 4.4.6 |

---

## 3. 数据库配置

### 3.1 当前配置（application-druid.yml）
```yaml
spring:
    datasource:
        druid:
            master:
                url: jdbc:mysql://localhost:3306/ry-vue?useUnicode=true&characterEncoding=utf8&zeroDateTimeBehavior=convertToNull&useSSL=true&serverTimezone=GMT%2B8
                username: root
                password: password
```

### 3.2 需要修改的配置
```yaml
# 建议修改为：
spring:
    datasource:
        druid:
            master:
                url: jdbc:mysql://localhost:3306/ry-cloud?useUnicode=true&characterEncoding=utf8&zeroDateTimeBehavior=convertToNull&useSSL=false&serverTimezone=GMT%2B8
                username: root
                password: root_password  # 与现有 .env 一致
```

### 3.3 数据库设计（CiliAI 业务表）
```
ry-cloud/                     # RuoYi 基础表
├── sys_user                # 用户表
├── sys_role                # 角色表
├── sys_menu                # 菜单表
├── sys_dept                # 部门表
├── sys_post                # 岗位表
├── sys_dict_type           # 字典类型表
├── sys_dict_data           # 字典数据表
├── sys_config              # 参数配置表
├── sys_notice              # 通知公告表
├── sys_oper_log            # 操作日志表
├── sys_logininfor          # 登录日志表
├── sys_job                  # 定时任务表
└── sys_job_log             # 定时任务日志表

ciliai/                      # CiliAI 业务表（新建）
├── cili_invite_code        # 邀请码表
├── cili_compute_power      # 用户算力表
├── cili_power_log          # 算力变动日志表
├── cili_works              # 作品表
├── cili_orders             # 订单表
├── cili_generation_record  # AI 生成记录表
├── cili_project            # 项目表
├── cili_advertisement      # 广告表
├── cili_chat_session       # 聊天会话表
└── cili_chat_message       # 聊天消息表
```

---

## 4. 下一步计划

### 4.1 备份现有代码
- [ ] 备份 Flask 后端代码（app.py、key_manager.py 等）
- [ ] 备份管理端前端（ciliAI/ruoyi/）
- [ ] 备份用户端前端（ciliAI/ciliAI/）
- [ ] 备份数据库配置和脚本

### 4.2 启动 MySQL 服务
```bash
# 启动 MySQL 服务
net start MySQL

# 或使用 Docker
docker run -d --name mysql -p 3306:3306 -e MYSQL_ROOT_PASSWORD=root_password mysql:8.0
```

### 4.3 初始化 RuoYi 数据库
```bash
# 创建数据库
CREATE DATABASE ry-cloud DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_general_ci;

# 导入 SQL 脚本
source d:/fangtang/RuoYi-Vue-master/sql/ry_20260321.sql;
source d:/fangtang/RuoYi-Vue-master/sql/quartz.sql;
```

---

## 5. 风险提示

⚠️ **重要提醒**：
1. 备份所有现有代码后再进行迁移
2. 确保数据库已正确配置和启动
3. 建议在测试环境中先验证 RuoYi 功能
4. 保持与现有 .env 配置的一致性

---

**报告生成人**：AI Assistant  
**下一步**：开始备份现有代码
