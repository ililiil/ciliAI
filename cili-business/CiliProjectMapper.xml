<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.system.mapper.CiliProjectMapper">

    <resultMap type="com.ruoyi.system.domain.CiliProject" id="CiliProjectResult">
        <id property="id" column="id" />
        <result property="name" column="name" />
        <result property="userId" column="user_id" />
        <result property="type" column="type" />
        <result property="description" column="description" />
        <result property="status" column="status" />
        <result property="createTime" column="create_time" />
        <result property="updateTime" column="update_time" />
    </resultMap>

    <sql id="selectCiliProjectVo">
        select id, name, user_id, type, description, status, create_time, update_time from cili_project
    </sql>

    <select id="selectCiliProjectList" parameterType="com.ruoyi.system.domain.CiliProject" resultMap="CiliProjectResult">
        <include refid="selectCiliProjectVo"/>
        <where>
            <if test="name != null and name != ''">
                AND name like concat('%', #{name}, '%')
            </if>
            <if test="type != null and type != ''">
                AND type = #{type}
            </if>
            <if test="status != null and status != ''">
                AND status = #{status}
            </if>
        </where>
        order by create_time desc
    </select>

    <select id="selectCiliProjectById" parameterType="Long" resultMap="CiliProjectResult">
        <include refid="selectCiliProjectVo"/>
        where id = #{id}
    </select>

    <insert id="insertCiliProject" parameterType="com.ruoyi.system.domain.CiliProject">
        insert into cili_project
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="name != null">name,</if>
            <if test="userId != null">user_id,</if>
            <if test="type != null">type,</if>
            <if test="description != null">description,</if>
            <if test="status != null">status,</if>
            create_time,
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="name != null">#{name},</if>
            <if test="userId != null">#{userId},</if>
            <if test="type != null">#{type},</if>
            <if test="description != null">#{description},</if>
            <if test="status != null">#{status},</if>
            now(),
        </trim>
    </insert>

    <update id="updateCiliProject" parameterType="com.ruoyi.system.domain.CiliProject">
        update cili_project
        <trim prefix="SET" suffixOverrides=",">
            <if test="name != null">name = #{name},</if>
            <if test="type != null">type = #{type},</if>
            <if test="description != null">description = #{description},</if>
            <if test="status != null">status = #{status},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
        </trim>
        where id = #{id}
    </update>

    <delete id="deleteCiliProjectByIds" parameterType="Long">
        delete from cili_project where id in
        <foreach item="id" collection="array" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

    <delete id="deleteCiliProjectById" parameterType="Long">
        delete from cili_project where id = #{id}
    </delete>

</mapper>
